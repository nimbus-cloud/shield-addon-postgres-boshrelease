#!/bin/sh
set -eux
CPUS=$(grep -c ^processor /proc/cpuinfo)

tar -xzf postgres/postgresql-13.7.tar.gz
cd postgresql-13.7
./configure --prefix=${BOSH_INSTALL_TARGET} --with-openssl
make -j$CPUS && make install
